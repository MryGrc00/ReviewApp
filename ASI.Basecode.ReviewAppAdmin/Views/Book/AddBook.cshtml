@model ASI.Basecode.Services.ServiceModels.BookViewModel


<h2>Add New Book</h2>

<form method="post" enctype="multipart/form-data" asp-action="AddBook">
    <div class="form-group">
        <label>Isbn</label>
        <input class="form-control" asp-for="Isbn" />
        <span asp-validation-for="Isbn" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label>Title</label>
        <input class="form-control" asp-for="Title" />
        <span asp-validation-for="Title" class="text-danger"></span>

    </div>
    <div class="form-group">
        <label>Author</label>
        <input class="form-control" asp-for="Author" />
        <span asp-validation-for="Author" class="text-danger"></span>

    </div>

    <div class="form-group">
        <label>Genres</label>
        @foreach (var genre in (List<ASI.Basecode.Services.ServiceModels.GenreViewModel>)ViewData["Genre"])
        {
            <div class="form-check">
                <input class="form-check-input" type="checkbox" name="SelectedGenres" value="@genre.GenreName" />
                <label class="form-check-label">@genre.GenreName</label>
            </div>
        }
        <span asp-validation-for="SelectedGenres" class="text-danger"></span>
        <input type="hidden" asp-for="Genre" id="selectedGenresInput" />
    </div>

    <div class="form-group">
        <label>Description</label>
        <textarea class="form-control" rows="3" asp-for="Description"></textarea>
        <span asp-validation-for="Description" class="text-danger"></span>

    </div>
    <div class="form-group">
        <label>Choose Image</label>
        <input asp-for="ImageFile" type="file" accept=".png" class="form-control-file" required/>
        <span asp-validation-for="ImageFile" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Add</button>
</form>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('input[type=checkbox]').change(function () {
                var selectedGenres = $('input[name="SelectedGenres"]:checked')
                    .map(function () {
                        return this.value;
                    })
                    .get();

                $('#selectedGenresInput').val(selectedGenres.join(', '));
            });
        });
    </script>
}

 